rootProject.name = 'account'

include 'account-api'
include 'account-api-starter'
include 'account-core'
include 'account-core-starter'
include 'account-ui'
include 'account-ui-starter'

rootProject.children.each { p1 ->
    p1.buildFileName = "${p1.name}.gradle"
}
